streamlit>=1.30.0
openpyxl>=3.1.0
pandas>=2.0.0
plotly>=5.18.0
Pillow>=10.0.0

# Pipeline real (OCR + clasificacion + NER)
pymupdf>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# OCR: PaddleOCR (matchea la distribucion de training de los modelos GLiNER).
# NO usar EasyOCR -- usa onnxruntime que entra en conflicto con torch (GLiNER)
# y produce segfault al cargar ambos en CPU Windows.
paddleocr==2.8.1
paddlepaddle==2.6.2

# NER GLiNER fine-tuned (4 modelos, ver models/ner/gliner/)
# Las versiones estan pineadas exactas a las del entrenamiento (notebook 24)
# para garantizar carga reproducible de los pesos.
gliner==0.2.13
transformers==4.45.0
