streamlit>=1.30.0
openpyxl>=3.1.0
pandas>=2.0.0
plotly>=5.18.0
Pillow>=10.0.0

# Pipeline real (OCR + clasificacion + NER)
pymupdf>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0

# OCR: PaddleOCR (matchea la distribucion de training de los modelos GLiNER).
# NO usar EasyOCR -- usa onnxruntime que entra en conflicto con torch (GLiNER)
# y produce segfault al cargar ambos en CPU Windows.
paddleocr==2.8.1
paddlepaddle==2.6.2

# NER VARIANTE v2: GLiNER2 unificado (modelo de las companeras,
# Yeraldyn/gliner2-large-sineria-v8). gliner2[local] trae torch, transformers
# y peft. NO se pinea transformers para dejar que gliner2 resuelva su version
# (el modelo se guardo con transformers 5.x).
gliner2[local]
